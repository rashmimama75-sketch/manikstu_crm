node_modules/
.next/
out/
.env*
*.log
.DS_Store
*.tsbuildinfo

# Copied from pdfjs-dist on npm install (see package.json postinstall)
public/pdf.worker.min.mjs

# Local data: staff added on the Staff onboarding page (contains password hashes)
# and the shared telecalling data (tracker.json) until the backend is connected
.data/
